const add = (first, second) => {
    return first + second;
}

// export default add;
const multifly = (first, second) => {
    return first * second;
}

const subtraction = (first, second) => {
    return first - second;
} 

export {add, multifly, subtraction };